[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bottle-vision"
version = "0.2.0"
description = "Segmentation-first bottle vision pipeline"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26,<2",
  "pydantic>=2.7,<3",
  "PyYAML>=6.0,<7",
  "Pillow>=10,<12",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "ruff>=0.6,<1",
]
ui = [
  "gradio>=5,<7",
]
sam3 = [
  "torch>=2.7",
  "torchvision>=0.22",
  "huggingface_hub>=0.30",
  "timm>=1.0.17",
  "tqdm",
  "ftfy==6.1.1",
  "regex",
  "iopath>=0.1.10",
  "typing_extensions",
  "einops>=0.8",
  "sam3 @ git+https://github.com/facebookresearch/sam3.git",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
